# WeiMD 对比报告 —— 可复制参考的调整

> 对比对象：[maoruibin/WeiMD](https://github.com/maoruibin/WeiMD)（下称 **WeiMD**） vs 本项目 **MWMD**
> 生成日期：2026-09-13 · 依据：WeiMD 仓库 README/概览 + 本项目 SPEC.md v1.2 与现有代码
> 结论先行：两者技术栈高度一致（React 18 + Vite + pnpm），但**定位相反**——WeiMD 走「联网 + 图床 + 全功能渲染」，MWMD 走「纯本地 + 占位 + Rust 原生单文件」。因此**只选择性参考**其纯前端、离线可用的增强点，坚决不引入任何联网/上传能力。

---

## 1. 技术栈与定位对比

| 维度 | WeiMD | MWMD（本项目） |
|---|---|---|
| 前端框架 | React 18 + Vite + pnpm | React 18 + Vite + pnpm（**一致**） |
| 外壳 | Web + Electron（跨平台） | **Tauri v2**（Rust 核心 + WebView2，Windows 优先） |
| 核心逻辑 | 前端 JS | **Rust**（渲染/剪贴板/文件/监听） |
| 渲染方式 | 前端渲染 + 自定义 CSS 主题 | Rust `pulldown-cmark` → **全内联样式**（微信只认内联） |
| 剪贴板 | 一键复制（前端 Clipboard） | **CF_HTML**（winapi 写 HTML Format）+ CF_UNICODETEXT 兜底 |
| 网络 | **需要**（图床上传、深色算法等） | **纯离线，零联网**（已确认非目标） |
| 分发 | Web 部署 + Electron 安装包 | **便携单文件 exe**（未签名）+ GitHub Releases 自替换更新 |
| 存储 | 本地 | 本地 .md + 多根工作区 + 目录树 + notify 热刷新 |

**判断**：技术栈选型被 WeiMD 侧面验证（同为 React18/Vite/pnpm）。但我们用 Tauri+Rust 换取更小体积、原生性能、单文件绿色分发，这是**差异化优势，不改**。

---

## 2. 功能矩阵

图例：✅ 已有 · 🟡 计划中 · ⛔ 刻意不做（违反已确认范围） · ❌ 暂无

| 功能 | WeiMD | MWMD | 说明 |
|---|---|---|---|
| GFM（表格/删除线/任务列表） | ✅ | ✅ | pulldown-cmark 已开启 |
| 文章主题数量 | 10+ | ✅ 17 | v1.5 扩充到 **17 套**（配色 8 + 出版风格 9），并按类别分组 |
| 自定义/导入主题 CSS | ✅ | ✅ v1.10（编辑器） | 可视化「主题编辑器」暴露版式枚举 + 12 项调色板，可基于预设载入/实时预览/存为自定义主题；内部仍生成内联样式串、微信安全。**导入第三方主题**（外部文件）暂未做 |
| 代码块语法高亮 | ✅ | ✅ v1.6 | syntect 内联着色 span；按代码底明暗自适应配色，无语言回退纯文本 |
| 一键复制到公众号 | ✅ | ✅ | 我们走 CF_HTML，保真度已实测验证 |
| 导出 HTML / PDF | ✅ | ✅ v1.7 | 同一份内联样式文档（带打印 CSS）；HTML 落盘、PDF 复用系统 WebView2 的 `PrintToPdf` **静默生成**（无打印对话框、零额外体积、与预览同引擎） |
| **微信深色模式预览** | ✅（98% 精度，mp-darkmode 算法） | ✅ v1.9 进阶版 | 独立「微信深色预览」开关（与界面外观解耦）；前端对内联颜色做 mp-darkmode 风格 HSL 映射；仅影响预览、不改输出 HTML |
| 全文/高级搜索 | ✅ | ✅ v1.8 | 侧栏「文件名/内容」双模式；内容模式 Rust 遍历 .md 返回文件+行号+片段 |
| LaTeX/公式**渲染** | ✅ | ⛔ | 我们**降级为编号占位**（已确认，不渲染/不嵌入） |
| 图床/图片上传（官方/七牛/阿里/腾讯） | ✅ | ⛔ | **明确放弃全部联网与上传**（已确认非目标） |
| 跨平台 Web | ✅ | ⛔ | 我们定位 Windows 原生单文件 |
| 全局界面深色 | ✅ | ✅ | chrome 全量同步；预览深色 v1.9 起解耦为独立「微信深色预览」开关（见上一行） |
| 工作区目录树 + 多根 + 热监听 | ❌/弱 | ✅ | **我们的优势** |
| 中英双语界面 | ? | ✅ | 内置零依赖字典 |

---

## 3. 重点：可以复制/参考的调整（按性价比排序）

### 3.1 ⭐ 微信深色模式预览（✅ v1.9 进阶版已实现）
- **WeiMD 做法**：基于 `wechatjs/mp-darkmode` 算法，宣称 98% 精度模拟文章在微信「深色模式」下的实际观感。
- **为什么值得参考**：公众号读者开启深色模式时，微信会对文章颜色做自动反色/调整。作者目前**看不到**深色下的效果，容易出现「浅色正常、深色刺眼/对比度丢失」。这与我们「预览==最终输出」的核心理念一致，且**纯客户端、可离线**，不违反零联网约束。
- **落地建议（MWMD 版）**：在预览区加一个「微信深色预览」开关（视图菜单或预览区标题栏按钮）。开启后对**预览画布**套一层近似算法：
  - 简化版：CSS `filter: invert(1) hue-rotate(180deg)` 对图片/代码块再做二次 invert 兜底；
  - 进阶版：参考 mp-darkmode 的规则，对内联样式里的颜色做映射（背景↔文字明度对调、强调色降饱和）。
- **注意**：只作用于**预览展示**，**绝不改变复制输出的 HTML**（复制仍用原内联样式）。我们当前「预览画布恒为白底」的设计需为此开关开一个例外分支。
- **成本**：中。简化版 0.5 天；规则映射版 1–2 天。

### 3.2 代码块语法高亮（已在 M4 计划，WeiMD 佐证必要性）
- **参考点**：WeiMD 已内置高亮，说明这是公众号排版刚需。
- **落地**：按 SPEC 既定方案用 **syntect** 在 Rust 侧输出**内联颜色 span**（微信剥离 class，必须内联）。与主题解耦：高亮配色可随主题走或固定一套深色代码底。
- **成本**：中，M4 已规划。

### 3.3 内容级/高级搜索（✅ v1.8 已实现）
- **参考点**：此前只有左侧树的**文件名**过滤；WeiMD 提供更强的搜索。
- **已落地**：侧栏搜索框加「文件名 / 内容」双模式。内容模式走 Rust `search_content` 遍历工作区所有 .md（跳过隐藏项）、大小写不敏感逐行匹配，返回「文件 + 行号 + 片段」（上限 500 条）；前端防抖 250ms、结果列表点击直达文档。纯本地、离线（详见 SPEC §4.13）。

### 3.4 自定义/导入主题（✅ v1.10 主题编辑器已实现）
- **差异**：WeiMD 用 CSS 主题；我们受微信约束只能用**内联样式映射**，不能直接照搬 CSS。
- **已落地**：视图菜单「主题编辑器…」暴露 `Spec` 全部旋钮——标题/引用/表格/分隔线版式枚举、首行缩进、行距、12 项调色板；可**基于任一预设载入**再改，**实时反映到右侧主预览**，存为**自定义主题**（`Config.custom_themes`，`custom:<id>` 选用，单列「自定义主题」分组）。自定义主题与预设共用同一 Rust 渲染管线（`resolve_theme`），输出仍纯内联样式、微信安全（详见 SPEC §4.15）。
- **未做**：导入第三方主题（外部 JSON/文件）留待后续。
- **成本**：中高，已交付。

---

## 4. 明确**不**参考的部分（范围护栏）

以下 WeiMD 能力**与本项目已确认的非目标冲突，坚决不引入**：

1. **图床 / 图片上传**（官方微信、七牛、阿里云、腾讯云）——违反「纯离线、不上传、不下载、不嵌入」；图片一律**编号占位块**。
2. **LaTeX / 公式渲染**——违反「公式/图表降级为占位」；任何微信不原生支持的内容统一占位 + 附源码。
3. **联网能力 / 微信 API**——无 access_token、无素材/草稿/发布、无 IP 白名单。
4. **跨平台 Web 部署 / Electron**——我们锁定 Tauri + Windows 便携单文件。

> 若未来要评估上述任一项，应先回到 SPEC §1.3「非目标」重新收口，而非直接实现。

---

## 5. 建议落地顺序

| 优先级 | 事项 | 归属 | 预估 |
|---|---|---|---|
| P0（已交付） | 17 套主题（配色 8 + 出版 9）、六级标题、全局深色 + 预览反相、工具栏分组、分区标题、折叠、帮助菜单、编辑器自动换行 | v1.2–v1.5 | — |
| P1 | 代码块语法高亮（syntect 内联） | M4-A ✅ v1.6 | — |
| P1 | 便携单文件打包 + GitHub Releases 自更新 + CI | M4-B 🟡 v1.10（CI+检查更新MVP；完整自替换待联网环境） | 中 |
| P2 | 微信深色模式预览 · **进阶版**（§3.1） | ✅ v1.9 | — |
| P2 | 内容级全文搜索（§3.3） | ✅ v1.8 | — |
| P3 | 可视化主题编辑器（§3.4） | ✅ v1.10 | — |

> **状态更新（v1.10，2026-09-14）**：本报告原依据 SPEC v1.2 撰写，现已按 v1.3–v1.10 实际进度校正——§3.1 **进阶版已交付**（v1.9，独立「微信深色预览」开关 + 前端 mp-darkmode 风格 HSL 颜色映射，替换 v1.3 的整块 CSS `invert` 简化方案；仅影响预览、不改输出 HTML）；§3.2 **代码块语法高亮已交付**（v1.6，syntect 内联 span，M4-A 完成）；§3.3 **内容级全文搜索已交付**（v1.8，侧栏文件名/内容双模式）；§3.4 **可视化主题编辑器已交付**（v1.10，暴露版式枚举 + 12 项调色板、基于预设载入/实时预览/存为自定义主题，与预设共用同一渲染管线；导入第三方主题未做）；导出 HTML/PDF 已交付（v1.7）；主题由 10 套扩到 **17 套**并分两类。**剩余仅 P1 的 M4-B（便携打包 + 自更新 + CI，#31）**。

**一句话**：WeiMD 值得我们抄的纯离线增强——「**全文搜索**」（v1.8）、「**微信深色预览**」（v1.9 进阶版）与「**可视化主题编辑器**」（v1.10）**均已交付**；高亮/导出/主题均已落地；图床、公式渲染、联网、跨平台**一律不碰**。
